<Window
  x:Class="EditModule.Views.ImageDropDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
  xmlns:converters="clr-namespace:System.Windows.Controls;assembly=PresentationFramework"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  Title="ImageDropDialog"
  Width="300"
  Height="300"
  AllowsTransparency="True"
  Background="Transparent"
  BorderThickness="0"
  ShowInTaskbar="False"
  WindowStyle="None"
  mc:Ignorable="d">

  <Window.Resources>
    <converters:BooleanToVisibilityConverter x:Key="BoolToVis" />
  </Window.Resources>

  <Border
    Background="{DynamicResource WindowBackgroundBrush}"
    BorderBrush="{DynamicResource TextBoxBorderBrush}"
    BorderThickness="1"
    Visibility="{Binding Uploading, Converter={StaticResource BoolToVis}}">
    <StackPanel
      Margin="4"
      VerticalAlignment="Center"
      Orientation="Horizontal">
      <controls:MetroProgressBar
        x:Name="ProgressBar"
        Width="90"
        Height="10"
        Margin="0,0,10,0"
        VerticalAlignment="Center" />
      <Button VerticalAlignment="Center" Content="Cancel" />
    </StackPanel>
  </Border>
</Window>
