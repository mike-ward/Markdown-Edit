<controls:MetroWindow
  x:Class="EditModule.Views.FindReplaceDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:mvvm="http://prismlibrary.com/"
  Title="Find and Relace"
  Width="300"
  Height="300"
  mvvm:ViewModelLocator.AutoWireViewModel="True"
  ResizeMode="NoResize"
  ShowInTaskbar="False"
  WindowStartupLocation="CenterOwner"
  WindowStyle="ToolWindow"
  mc:Ignorable="d">
  <Grid Margin="0,4">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>

    <TabControl
      Name="_tabMain"
      Grid.ColumnSpan="2"
      Height="Auto">
      <TabItem Header="Find">
        <StackPanel>
          <TextBlock Margin="3">Text to Find:</TextBlock>
          <TextBox
            Name="_textFind"
            Margin="3"
            Text="{Binding FindText}" />
          <Button
            Width="80"
            Margin="5"
            HorizontalAlignment="Right"
            Click="FindNextClick"
            Content="Find Next" />
        </StackPanel>
      </TabItem>
      <TabItem Header="Replace">
        <StackPanel>
          <TextBlock Margin="3">Text to Find:</TextBlock>
          <TextBox
            Name="_textFind2"
            Margin="3"
            Text="{Binding FindText}" />
          <TextBlock Margin="3" Text="Replace with:" />
          <TextBox Margin="3" Text="{Binding ReplaceText}" />
          <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
            <Button
              Width="80"
              Margin="5"
              HorizontalAlignment="Right"
              Click="FindNextClick"
              Content="Find Next" />
            <Button
              Width="80"
              Margin="5"
              HorizontalAlignment="Right"
              Click="ReplaceClick"
              Content="Replace" />
            <Button
              Width="80"
              Margin="5"
              HorizontalAlignment="Right"
              Click="ReplaceAllClick"
              Content="Replace All" />
          </StackPanel>
        </StackPanel>
      </TabItem>
    </TabControl>

    <CheckBox
      Grid.Row="1"
      Grid.Column="0"
      Margin="10,2"
      Checked="{Binding CaseInsensitive}"
      Content="Match case"
      IsChecked="true" />
    <CheckBox
      Grid.Row="2"
      Grid.Column="0"
      Margin="10,2"
      Checked="{Binding WholeWord}"
      Content="Match whole word"
      IsChecked="true" />
    <CheckBox
      Grid.Row="1"
      Grid.Column="1"
      Margin="10,2"
      Checked="{Binding UseRegEx}"
      Content="Regular Expression" />
    <CheckBox
      Grid.Row="2"
      Grid.Column="1"
      Margin="10,2"
      Checked="{Binding Wildcards}"
      Content="Wildcards" />
    <CheckBox
      Grid.Row="3"
      Grid.Column="1"
      Margin="10,2"
      Checked="{Binding SearchUp}"
      Content="Search up" />
  </Grid>
</controls:MetroWindow>
